version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - cd portfolio
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: portfolio/.next
    files:
      - '**/*'
  cache:
    paths:
      - portfolio/node_modules/**/*